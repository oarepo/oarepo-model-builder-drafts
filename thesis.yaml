model:
  properties:
    metadata:
      properties:
        title:
          type: fulltext
        status:
          type: keyword
  permissions:
    presets:
    - everyone
  package: thesis
  schema-server: 'local://'
  use:
  - invenio